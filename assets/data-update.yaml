apiVersion: deploy.xzd5/v1
kind: DataPatch

metadata:
  app: sample-app
  revision: 2026.08.05.1
  description: 示例数据更新

spec:
  # Replace only after the active manifest and DATA_SAFETY.md inventory prove
  # this exact subtree is operator-managed and disjoint from protected data.
  target: operator-resources
  mode: merge
  delete: []
